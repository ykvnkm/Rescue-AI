[pytest]
testpaths = tests
markers =
    integration: integration tests that require running infrastructure
addopts = --cov=services/api_gateway --cov=libs/batch --cov=services/batch_runner --cov-report=term-missing --cov-fail-under=75

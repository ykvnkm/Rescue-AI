# ADR-0005: Формат артефакта модели и стратегия рантайма

- Статус: Предложено
- Дата: 2026-03-04
- Авторы: Максим Яковенко, Михаил Скрыпник, Иван Провков

## Контекст
В MVP важны быстрые итерации (перебор моделей/конфигов, анализ ошибок). В production важны производительность и переносимость на edge.

## Решение
В MVP:
- основной артефакт модели — PyTorch (.pt) для скорости экспериментов.
- хранить версионированные артефакты (model + config alerting + manifest миссий).

В production:
- предусмотреть экспорт в ONNX и/или OpenVINO (если упираемся в latency/FPS на edge),

## Рассмотренные альтернативы
- TorchScript как основной формат.
- Только ONNX/OpenVINO с самого начала.
- Только TensorRT (как привязка к NVIDIA).

## Последствия
Плюсы:
- MVP быстрый в разработке и проще дебажить.
- Есть путь оптимизации под слабое железо, если понадобится.

Минусы:
- Экспорт требует отдельного пайплайна валидации (сравнить метрики до/после экспорта).

## Ссылки
- [Контракт рантайма: откуда берётся model_url и параметры инференса](../../services/detection_service/infrastructure/runtime_contract.py)
- [YOLO runtime/adapter (точка смены формата/рантайма при необходимости)](../../services/detection_service/infrastructure/yolo_detector.py)
- [Зависимости проекта (extras, inference stack)](../../pyproject.toml)
- [Dockerfile (упаковка сервиса)](../../Dockerfile)
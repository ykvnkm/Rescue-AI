artifact_store:
  provider: s3
  endpoint_url: http://minio:9000
  bucket: rescue-ai-artifacts
  region: us-east-1
  access_key_env: S3_ACCESS_KEY
  secret_key_env: S3_SECRET_KEY
  path_prefix: missions/


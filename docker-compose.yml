services:
  api:
    image: ${ONLINE_IMAGE}
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      APP_HOST: "0.0.0.0"
      APP_PORT: "8000"
    volumes:
      - model_cache:/app/runtime/models

volumes:
  model_cache:

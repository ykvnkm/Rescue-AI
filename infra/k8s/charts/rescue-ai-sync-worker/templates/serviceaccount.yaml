{{- if .Values.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rescue-ai-sync-worker.serviceAccountName" . }}
  labels:
    {{- include "rescue-ai-sync-worker.labels" . | nindent 4 }}
{{- end }}

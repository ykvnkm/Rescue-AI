{{- if .Values.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rescue-ai-batch-exporter.serviceAccountName" . }}
  labels:
    {{- include "rescue-ai-batch-exporter.labels" . | nindent 4 }}
{{- end }}

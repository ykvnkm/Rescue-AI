{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rescue-ai-nav-engine.fullname" . }}
  labels:
    {{- include "rescue-ai-nav-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rescue-ai-nav-engine.selectorLabels" . | nindent 4 }}
{{- end }}

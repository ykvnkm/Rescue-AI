{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rescue-ai-detection.fullname" . }}
  labels:
    {{- include "rescue-ai-detection.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rescue-ai-detection.selectorLabels" . | nindent 4 }}
{{- end }}
